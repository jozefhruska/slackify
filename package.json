{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "version": "lerna version --conventional-commits --yes",
    "postinstall": "npm run bootstrap",
    "heroku-postbuild": "sh deployment/postbuild.sh"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
