# Migration `20200523222310-edit-component-data-relation`

This migration has been generated by Jozef Hruška at 5/23/2020, 10:23:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Component" DROP CONSTRAINT IF EXiSTS "DeleteLinkComponentData";

ALTER TABLE "public"."StatRecord" DROP CONSTRAINT IF EXiSTS "OnComponentDelete";

CREATE UNIQUE INDEX "Component_plainTextDataId" ON "public"."Component"("plainTextDataId")

CREATE UNIQUE INDEX "Component_articleDataId" ON "public"."Component"("articleDataId")

CREATE UNIQUE INDEX "Component_linkDataId" ON "public"."Component"("linkDataId")

ALTER TABLE "public"."StatRecord" ADD FOREIGN KEY ("componentId")REFERENCES "public"."Component"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200523170619-statrecord--relation-with-component-nullable..20200523222310-edit-component-data-relation
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
   enabled  = env("DATABASE_URL")
 }
 generator cient_common {
@@ -75,26 +75,26 @@
   @@unique([createdAt, componentId])
 }
 model PlainTextComponentData {
-  id        String      @default(cuid()) @id
+  id        String    @default(cuid()) @id
   text      String
-  Component Component[]
+  Component Component
 }
 model ArticleComponentData {
-  id        String      @default(cuid()) @id
+  id        String    @default(cuid()) @id
   title     String
   lead      String?
   content   String
-  Component Component[]
+  Component Component
 }
 model LinkComponentData {
-  id        String      @default(cuid()) @id
+  id        String    @default(cuid()) @id
   text      String?
   url       String
-  Component Component[]
+  Component Component
 }
 enum UserRole {
   OWNER
```


