# Migration `20200209153448-add-category-model`

This migration has been generated by Jozef Hruška at 2/9/2020, 3:34:48 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
    "accessToken" text  NOT NULL DEFAULT '',
    "email" text  NOT NULL DEFAULT '',
    "id" text  NOT NULL ,
    "name" text  NOT NULL DEFAULT '',
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Category" (
    "handle" text  NOT NULL DEFAULT '',
    "id" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Post" (
    "author" text  NOT NULL ,
    "category" text  NOT NULL ,
    "content" text   ,
    "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    "id" text  NOT NULL ,
    "isPublished" boolean  NOT NULL DEFAULT false,
    "short" text  NOT NULL DEFAULT '',
    "title" text  NOT NULL DEFAULT '',
    "type" text  NOT NULL DEFAULT '',
    "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "User.accessToken" ON "public"."User"("accessToken")

ALTER TABLE "public"."Post" ADD FOREIGN KEY ("author") REFERENCES "public"."User"("id") ON DELETE RESTRICT

ALTER TABLE "public"."Post" ADD FOREIGN KEY ("category") REFERENCES "public"."Category"("id") ON DELETE RESTRICT
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200209153448-add-category-model
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,37 @@
+datasource db {
+  provider = "postgresql"
+  url      = env("POSTGRES_URL")
+  enabled  = env("POSTGRES_URL")
+  // url      = "postgresql://postgres:password@localhost:5432/postgres"
+}
+
+generator photon {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id          String @id
+  email       String @unique
+  name        String
+  accessToken String @unique
+  posts       Post[]
+}
+
+model Category {
+  id          String @default(cuid()) @id
+  handle      String
+  posts       Post[]
+}
+
+model Post {
+  id          String   @default(cuid()) @id
+  type        String
+  title       String
+  short       String
+  content     String?
+  isPublished Boolean
+  category    Category
+  author      User
+  createdAt   DateTime @default(now())
+  updatedAt   DateTime @updatedAt
+}
```


